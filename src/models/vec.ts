export interface Vec2 {
  col: number;
  row: number;
}

export interface Position {
  x: number;
  y: number;
}

export interface BrickQuadrants {
  tl: boolean;
  tr: boolean;
  bl: boolean;
  br: boolean;
}
